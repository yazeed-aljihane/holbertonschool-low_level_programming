#include "main.h"


/**
 * _isupper - check latter case
 * @c: input latter
 * Return: return 1 if uppercase and 0 otherwaise
 */

int _isupper(int c)
{
if (c >= 'A' && c <= 'Z')
{
return (1);
}
else
{
return (0);
}
}
