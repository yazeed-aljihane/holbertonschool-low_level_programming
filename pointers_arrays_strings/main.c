#include "main.h"
#include <stdio.h>

/**
 * main - check the code
 *
 * Return: Always 0.
 */
int main(void)
{
char n = '0';
int i;
char g = '3';
for (n;n != g;n++)
{
i++;
}
printf ("%d", i);
putchar(n * 10);

return (0);
}
